public:
  env_dim: &env_dim 16
  action_dim: &action_dim 4

# Imitation Learning Trainer
output_dir: "outputs/"
from_checkpoint: "" # Initial model checkpoint for training, different from resume checkpoint
data_files: ["data/human_2048.json", "data/agent_2048.json"] # Demonstration data files
data_threshold_steps: 550
train_epoch: &imitation_train_epoch 2000
batch_size: 128

# Learning rate schedule
learning_rate:
  warmup_rate: 0.1
  total_steps: *imitation_train_epoch
  eta_min: 1.0e-7
  eta_max: 1.0e-4

# Other training settings
optimizer: AdamW
log_interval: 10
save_interval: 100
